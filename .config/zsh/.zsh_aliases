# git config alias
alias config='/usr/bin/git --git-dir=/Users/alexfedosov/.cfg/ --work-tree=/Users/alexfedosov'

# react-native android aliases
alias androidDebug='react-native run-android --variant=mobileDebug'
alias androidRelease='react-native run-android --variant=mobileRelease'

# utility functions
alias myip='ipconfig getifaddr en0'


# tmux
alias t='tmux'
alias ta='tmux attach -t'
alias tw='tmux rename-window'
alias ts='tmux rename-session'

# adb
alias pixel='adb -s 996AY1932G'

# use exa instead of ls
alias ll='exa -l --sort=filename'
alias ls='exa'

# use neovim
alias v='nvim'

